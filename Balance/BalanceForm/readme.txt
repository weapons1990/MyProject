注意：
一、自动插入数据时：
1、配置文件中StatisticalDay为25，则数据为上个月25号（包括上月的25号数据）到本月25号（不包括本月25号）
   tz_Balance中为本月的月份
2、配置文件中StatisticalDay为1，则数据为上个月1号（包括上月的25号数据）到本月1号（不包括本月25号）
   tz_Balance中为本月的月份
二、手动插入数据时：
1、配置文件中StatisticalDay为25，tz_Balance中为文本框中的月份
2、配置文件中StatisticalDay为1，tz_Balance中为文本框中的月份的上一个月份